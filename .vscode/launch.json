{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "debug",
            "type": "cppdbg",
            "request": "launch",
            "cwd": "${workspaceFolder}",
            "program": "${workspaceFolder}/bin/a.out",
            "envFile": "${workspaceFolder}/.vscode/.env",
            "MIMode": "gdb",
            "preLaunchTask": "build"
        }
    ]
}